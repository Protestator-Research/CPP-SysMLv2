// Generated by sysml/tools/generate_abstract_syntax.py.
#include <sysml/flows/FlowDefinition.h>
#include <sysml/usage/Usage.h>
#include <algorithm>
#include <stdexcept>

namespace SysMLv2::Entities {

std::vector<std::shared_ptr<Usage>> FlowDefinition::flowEnd() const {
    return flowEnd_;
}

void FlowDefinition::setFlowEnd(std::vector<std::shared_ptr<Usage>> value) {
    if (std::any_of(value.begin(), value.end(), [](const auto& item) { return !item; })) {
        throw std::invalid_argument("FlowDefinition.flowEnd: null entry");
    }
    for (std::size_t i = 0; i < value.size(); ++i) {
        if (std::find(value.begin(), value.begin() + i, value[i]) != value.begin() + i) {
            throw std::invalid_argument("FlowDefinition.flowEnd: duplicate entry");
        }
    }
    flowEnd_ = std::move(value);
}

void FlowDefinition::appendFlowEnd(std::shared_ptr<Usage> value) {
    auto values = flowEnd();
    values.push_back(std::move(value));
    setFlowEnd(std::move(values));
}

FlowDefinition::FlowDefinition() {
    _dType = "FlowDefinition";
}

FlowDefinition::~FlowDefinition() = default;

std::string_view FlowDefinition::metaclassName() const noexcept {
    return "FlowDefinition";
}

std::vector<RepresentationIssue> FlowDefinition::validateRepresentation(bool includeDerived) const {
    std::vector<RepresentationIssue> issues; (void)includeDerived;
    return issues;
}

} // namespace SysMLv2::Entities
